# Canvas Application Environment Variables
# Copy this file to .env in the canvas/ directory and modify as needed

# API URL for the RA1 backend (default: http://localhost:3001)
# When running locally with Docker backend, use the default
# When running in production, set to your deployed API URL
VITE_API_URL=http://localhost:3001